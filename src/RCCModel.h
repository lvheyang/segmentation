#pragma once

#include <list>
#include "Constraint.h"

using namespace std;


class RCCModel {
	public :
		//virtual list<RCCModel> single_segmentation( const Constraint& ) const = 0;
		//virtual RCCModel& delete_isolate_node( ) const = 0;
};	

